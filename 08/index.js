/***C:** comprimento do círculo \
**A:** área do círculo \
**R:** raio do círculo*/
const r = 1;
const pi = 3.1415;
const c = 2 * pi * r;
const a = pi * r * r;
console.log(`Para um circulo com o Raio de ${r}. O Comprimento do circulo é: ${c.toFixed(2)} , e a Area do cicurlo é: ${a.toFixed(2)}`);
