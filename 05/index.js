const x1 = 1;
const x2 = 1;
const y1 = 1;
const y2 = 4;

const distancia = Math.sqrt((x2 - x1) ^ 2 + (y2 - y1) ^ 2);
console.log(`A distância entre esses 2 pontos dados é de: ${distancia.toFixed()}`);
